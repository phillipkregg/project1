<%= simple_form_for(@sketch) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
  	<%= f.input :sketch_name %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
