<%= simple_form_for(@zone) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :zone_name %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
